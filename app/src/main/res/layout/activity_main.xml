<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.jessemitchell.myappportfolio.MainActivity">

    <TextView
        android:layout_width="300sp"
        android:layout_height="wrap_content"
        android:text="@string/app_title"
        android:textAlignment="center"
        android:textSize="25sp"
        android:id="@+id/textView"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        />

    <Button
        style="@style/buttons"
        android:text="@string/popular_movies"
        android:layout_below="@+id/textView"
        android:id="@+id/movies"
        android:onClick="popularMovies"/>

    <Button
        style="@style/buttons"
        android:text="@string/stock_hawk"
        android:layout_below="@+id/movies"
        android:id="@+id/stock"
        android:onClick="stockHawk"/>

    <Button
        style="@style/buttons"
        android:text="@string/build_bigger"
        android:layout_below="@+id/stock"
        android:id="@+id/bigger"
        android:onClick="buildBigger"/>

    <Button
        style="@style/buttons"
        android:text="@string/app_material"
        android:layout_below="@+id/bigger"
        android:id="@+id/material"
        android:onClick="appMaterial"/>

    <Button
        style="@style/buttons"
        android:text="@string/go_ubiquitous"
        android:layout_below="@+id/material"
        android:id="@+id/ubiquitous"
        android:onClick="ubiquitous"/>

    <Button
        style="@style/buttons"
        android:text="@string/capstone"
        android:layout_below="@+id/ubiquitous"
        android:id="@+id/capstone"
        android:onClick="capstone"/>
</RelativeLayout>
